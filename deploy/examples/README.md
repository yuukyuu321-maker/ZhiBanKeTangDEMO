# 部署配置示例

本目录只存放脱敏、无凭据、不可直接用于生产的配置示例。真实学校拓扑、硬件、域名、证书和服务等级必须通过现场评估确定。

- `textbook-review-decision.example.json`：教材人工复核决定结构；
- `textbook-assignment-catalog.example.json`：合成教材版本、适用关系和主体授权的本地开发目录。

示例中的 `active` 只用于合成身份，不得复制到仍为 `needs_review` 的真实教材。
