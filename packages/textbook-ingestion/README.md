# 教材导入与本地检索

该包实现教材 PDF 的本地登记、SHA-256 指纹、逐页标签读取、文本与坐标提取、页图渲染、证据锚点、导入报告和确定性关键词检索。

原始教材不会复制进仓库或导入产物；导入目录只保存可删除的派生产物。每次真实导入必须显式填写来源、授权范围和操作者。

运行入口和人工抽查流程见 `07_Technology/School_AI/MVP/Increment_1_Textbook_Import_Runbook.md`。
